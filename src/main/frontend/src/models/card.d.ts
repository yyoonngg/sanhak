type AiCard = {
    fromDate?: string,
    toDate?: string,
    title?: string,
    category?: string[],
    skills?: Skill[],
    tools?: string[],
    reflection?: string,
    imageUrl?: string,
    pdfFile?: string,
    sourceUrl?: string[]
}